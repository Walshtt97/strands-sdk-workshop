# AWS Configuration
# Copy this file to .env and fill in your AWS credentials

# Choose ONE of the following options:

# Option 1: Use AWS Profile (recommended if you have AWS CLI configured locally)
# Uncomment the line below AND uncomment the volume mount in docker-compose.yml
# AWS_PROFILE=default

# Option 2: Use explicit credentials (recommended for CI/CD or when no local AWS CLI)
# Uncomment and fill in the lines below:
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# AWS Region (required for both options)
AWS_REGION=us-east-1

# Jupyter Configuration (optional)
# JUPYTER_TOKEN=your_custom_token_here